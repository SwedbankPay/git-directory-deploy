name: Test

on: [push, pull_request]

jobs:
  test:
    name: Test
    runs-on: ubuntu-latest

    steps:
    - uses: actions/checkout@v2

    - name: Setup BATS
      uses: mig4/setup-bats@master
      with:
        bats-version: 1.1.0

    - name: Run BATS test
      run: bats . lib
